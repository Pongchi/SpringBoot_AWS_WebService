spring:
    jpa:
        show_sql: true

    h2:
        console:
            enabled: true

    profiles:
        include: oauth

    session:
        store-type: jdbc

server:
    servlet:
        encoding:
            force-response: true

    use-forward-headers: true